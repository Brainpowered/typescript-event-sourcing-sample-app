{
  "watch": ["src/**/*.ts"],
  "ext": "ts",
  "exec": "rm -rf dist && tsc --build --force && concurrently --raw \"node -r dotenv/config -r tsconfig-paths/register dist/api/index.js\" \"node -r dotenv/config -r tsconfig-paths/register dist/subscribers/index.js\""  
}